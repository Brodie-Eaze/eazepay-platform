{
  "name": "webhooks-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/webhooks/src",
  "projectType": "application",
  "tags": ["scope:app", "type:webhook-receiver"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm --filter @eazepay/webhooks-app build" }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm --filter @eazepay/webhooks-app dev" }
    }
  }
}
