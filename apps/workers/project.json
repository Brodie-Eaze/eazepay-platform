{
  "name": "workers",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/workers/src",
  "projectType": "application",
  "tags": ["scope:app", "type:worker"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "pnpm --filter @eazepay/workers build" } },
    "dev": { "executor": "nx:run-commands", "options": { "command": "pnpm --filter @eazepay/workers dev" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "pnpm --filter @eazepay/workers typecheck" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "pnpm --filter @eazepay/workers lint" } }
  }
}
