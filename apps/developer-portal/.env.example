# developer-portal env template — copy to .env.local
#
# This app currently has no required env vars. The EAZEPAY_KEY shown
# in code samples on the page is documentation, not a runtime config
# value of this app itself.
