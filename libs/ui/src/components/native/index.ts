/**
 * React Native counterparts of the web components. Same prop surface,
 * different render. To be filled in when apps/consumer-mobile takes a
 * dependency — at that point the RN components import from
 * 'react-native' and use Pressable / View / Text. Today this file
 * exists as the entry point so consumers can import from
 * '@eazepay/ui/native' without errors when the file is empty.
 */
export {};
